# 参与贡献

感谢你对 DurianFlow Agent 的兴趣。

## 开发流程

1. Fork 仓库并从 `main` 创建功能分支。
2. 不要提交真实客户数据、授权文件、API Key、数据库、日志或本机配置。
3. 修改业务规则时同步增加单轮或多轮回归用例。
4. 提交前运行：

```powershell
python -m unittest discover -s tests -v
```

5. Pull Request 需要说明修改目的、行为变化、验证方式及可能影响的业务边界。

## 代码原则

- 价格、库存、运费、售后、合同和赔付等事实必须来自可追溯规则，不能交给模型自由编造。
- 外部动作要可审计、可去重、可暂停，并在结果不确定时失败关闭。
- 不接受验证码绕过、风控规避、账号接管、隐私数据买卖或未经授权批量营销相关功能。
- 示例数据必须完全虚构，测试手机号应使用明显的占位号码。
