# 安全说明

## 报告安全问题

请不要在公开 Issue 中提交 API Key、微信账号、客户名单、手机号、收货地址、订单数据、数据库文件或授权材料。发现代码安全问题时，请使用 GitHub Security Advisory 的私密报告功能联系维护者。

## 必须保密的内容

- `data/` 中的运行数据库及其 WAL/SHM 文件；
- `config/worker.json` 中的节点信息和加密令牌；
- `rpa/authorization.json` 及项目外保存的授权文件；
- DeepSeek API Key、中央端管理口令和节点注册口令；
- 导入的 CSV/Excel 客户资料、订单、地址、电话和物流单号；
- `work/`、日志、备份、发布包以及任何本机浏览器资料。

这些路径已加入 `.gitignore`，但提交前仍应执行人工复核和密钥扫描。密钥一旦误传，应立即在对应平台吊销并重新生成；仅从 Git 历史删除不能恢复密钥安全。

## 部署边界

- 中央端不要直接暴露到公网，应放在可信局域网、VPN或受控的 HTTPS 反向代理之后。
- 生产环境必须配置 `AGENT_ADMIN_TOKEN` 和 `AGENT_BOOTSTRAP_TOKEN`。
- RPA 必须运行在独立、已登录且未锁屏的 Windows 交互桌面。
- 验证码、安全提示、联系人不唯一、消息方向不明确或发送结果不确定时必须停止，不得自动绕过。
- 启用自动触达前，使用者必须自行确认平台授权、联系人来源、隐私告知、营销许可和适用法律要求。
